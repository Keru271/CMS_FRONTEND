import React from 'react';
import { SEOStudio } from '@/src/components/cms/SEOStudio';

export const metadata = {
  title: 'SEO Governance & Meta Optimization | OmniStore CMS',
  description: 'Manage store global SEO, OpenGraph cards, SERP previews, and per-product search rankings.',
};

export default function SeoPage() {
  return <SEOStudio />;
}
